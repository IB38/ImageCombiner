using Avalonia.Controls;

namespace ImageCombiner.App.Views;

public partial class MainWindow : Window
{
    public const float DefaultWidth = 800;
    public const float DefaultHeight = 800;
    public MainWindow()
    {
        InitializeComponent();
    }
}